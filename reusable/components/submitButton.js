import { Button, Input } from "@nextui-org/react";
export default function SubmitButton() {
  return (
    <div className="justify-center flex grid">
      <Button size="sm">Submit</Button>
    </div>
  );
}
